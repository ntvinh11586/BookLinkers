<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vinh.booklinkers" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccountCreatingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BooksHavingActivity"
            android:label="@string/title_activity_books_having"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BooksNeedingActivity"
            android:label="@string/title_activity_books_needing"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BookInformationActivity"
            android:label="@string/title_book_information"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/AppTheme" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vinh.booklinkers.LoginActivity" />
        </activity>
        <activity
            android:name=".BookEditingActivity"
            android:label="@string/title_book_editing" />
        <activity
            android:name=".BookAddingActivity"
            android:label="@string/title_activity_book_adding" />
        <activity
            android:name=".YourInformationActivity"
            android:label="@string/title_activity_your_information" />
        <activity
            android:name=".OwnerInformationActivity"
            android:label="@string/title_activity_owner_information" />
        <activity
            android:name=".BooksSearchingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BooksExchangeDetailActivity"
            android:label="@string/title_activity_book_exchange_detail" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".OwnerDirectionMapsActivity"
            android:label="@string/title_activity_owner_direction_maps" />
        <activity android:name=".YourInformationEditingActivity" />
    </application>

</manifest>