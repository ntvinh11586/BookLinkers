<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vinh.booklinkers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountCreatingActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HavingBooksActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NeedingBooksActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BookInformation"
            android:theme="@style/AppTheme"
            android:parentActivityName="com.example.vinh.booklinkers.LoginActivity" >

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vinh.booklinkers.LoginActivity" />

        </activity>
    </application>

</manifest>